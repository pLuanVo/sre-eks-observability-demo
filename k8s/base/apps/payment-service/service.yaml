apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: demo
  labels:
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/part-of: sre-demo
spec:
  selector:
    app.kubernetes.io/name: payment-service
  ports:
    - port: 8082
      targetPort: http
      protocol: TCP
      name: http
