apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: demo
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: sre-demo
spec:
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
